OpenEXR==3.3.5
holodex==0.11.11
termcolor==3.1.0
imageio[ffmpeg]==2.37.0
hydra-core>=1.3.2
iopath>=0.1.10
pillow>=9.4.0
# Grounded-SAM2 auto-prompting: GroundingDINO (text -> box) via HuggingFace,
# so SAM2 can be seeded automatically (no clicks / borrowed masks). The HF
# route avoids compiling GroundingDINO's custom CUDA ops.
# Pin: GroundingDINO support landed in transformers 4.40, but the latest
# transformers requires torch>=2.2 while this image ships torch 2.1.0 (it would
# report "PyTorch was not found"). 4.44.2 supports GroundingDINO AND torch 2.1.
transformers==4.44.2
timm>=0.9.16